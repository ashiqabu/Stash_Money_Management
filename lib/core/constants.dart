import 'package:flutter/material.dart';

Widget kHeight(double height) {
  return SizedBox(
    height: height,
  );
}

Widget kWidth(double width) {
  return SizedBox(
    width: width,
  );
}

const Color mainColor = Color.fromARGB(225, 53, 9, 85);
